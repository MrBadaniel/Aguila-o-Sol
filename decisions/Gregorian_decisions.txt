#Select patron saint

decisions = {
	select_Gregorian_patron_saint = {
		is_high_prio = yes
		only_rulers = yes
		
		potential = {
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
			religion = Gregorian
			is_adult = yes
			has_Gregorian_patron_saint_trigger = no
			NOT = {has_character_flag = Gregorian_select}
			NOT = {has_character_modifier = Gregorian_Saint_Michael}
			NOT = {has_character_modifier = Gregorian_Saint_John}
			NOT = {has_character_modifier = Gregorian_Saint_Kateri}
			NOT = {has_character_modifier = Gregorian_Saint_Unknown}
		}
		allow = {
			block_general_event_trigger = no
			piety = 50
			NOT = {
				trait = cynical
			}
		}
		effect = {
			character_event = { id = newGregorian.1 }
			piety = -50
			set_character_flag = Gregorian_select
		}
					revoke_allowed = {
				always = no
        }
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.3 # Slow it down a little
			}
		}
	}
	claim_dominion = {
		is_high_prio = yes
		only_rulers = yes
		ai_check_interval = 6
		potential = {
		religion = Gregorian
			NOT = {
				has_landed_title = d_gregorian
			}
		}
		allow = {
			NOT = { trait = slow }
			NOT = { trait = dwarf }
			NOT = { trait = leper }
			NOT = { trait = blinded }
			NOT = { trait = imbecile }
			NOT = { trait = eunuch }
			NOT = { trait = incapable }
			NOT = { trait = content }
			prestige = 1000
			piety = 1000
			higher_real_tier_than = DUKE
			independent = yes
				OR = {
					martial = 15
					trait = skilled_tactician
					trait = brilliant_strategist
					}
		}
		effect = {
			prestige = -1000
			piety = -1000
			
				if = {
					limit = {
				d_gregorian = { has_holder = no } 
					}
				grant_title = d_gregorian
				
				#any_playable_ruler = {
				#limit = { religion = Gregorian }
						character_event = { id = AoM.50	days = 3 }
						#}
				}
				else = {
				add_claim = d_gregorian
					#any_playable_ruler = {
						#limit = { religion = Gregorian }
							character_event = { id = AoM.51	days = 3 }
							#}
				}
		
		}
		
	}	
}